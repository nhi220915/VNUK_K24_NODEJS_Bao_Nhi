// cach thong thuong

function hello(name, message){
    console.log("Chao" + name + ", ban that" + message);
}

// ES6

var hello = (name, message) => {
    console.log("Chao" + name + ", ban that" + message);
};

hello('My', 'dep gai hihi');